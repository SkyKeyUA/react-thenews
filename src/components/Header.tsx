/** @format */

import React from 'react';
import { Search } from './Search';

export const Header: React.FC = () => {
  return (
    <header className="header">
      <Search />
    </header>
  );
};
